
apiVersion: apps/v1
kind: Deployment
metadata:
  name: flower
  labels:
    deployment: flower
spec:
  replicas: 1
  selector:
    matchLabels:
      pod: celery-flower
  template:
    metadata:
      labels:
        pod: celery-flower
    spec:
      containers:
      - name: flower
        image: localhost:5000/satellitetracker:0.1.7
        command: ['celery', 'flower', '-A', 'satellite_tracker']
        ports:
          - containerPort: 5555
        env:
            - name: REDIS_HOST
              value: redis-service
        resources:
          limits:
            cpu: 100m
